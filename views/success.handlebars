<div>
    <p>Hi, {{username}}, your order is successfully processed.</p>
    <p>The details are as follows:</p>
    {{#each items as |item|}}
    <div style="background-color: #FF577F; margin: 2px">
        
        <p>Product Name: {{item.product.product_name}}</p>
        <p>Price: £{{item.product.price}}</p>
        <p>Quantity: {{item.quantity}}</p>
  
    </div>
    {{/each}}
    <div style="margin: 2px">
        <p>The items will be delivered to the following address:</p>
        <p>{{address}}</p>
        <p>An email will be sent to {{email}} for confirmation.</p>
    </div>    
</div>