<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WearCorp E-shop</title>  
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div>
    <header style="display: flex; align-items: center; background-color: #1693e7;">
    <!-- Add the logo image with a class -->
      <a href="/">
  <img src="/product-images/wearco-logo.png" alt="WearCorp Logo" class="logo-image" style="margin-right: 10px;" />
         </a>

      <!-- Navigation links -->
      <nav style="display: flex; flex-grow: 1;">
        {{#if logged_in}}
        <button id="logout">Logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
        <a href="/">Home</a>
        <a href="/categories">Category</a>
        <a href="/basket">Basket</a>
      </nav>
    </header>

    <main style="background-color: #ffffff">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>    
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}  
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
